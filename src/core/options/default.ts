import { ChangelogOptions, ReleaseOptions } from '~types'

export const changelogOptions: ChangelogOptions = {
  write: false,
  root: false,
  from: undefined
}

export const releaseOptions: ReleaseOptions = { from: undefined }
